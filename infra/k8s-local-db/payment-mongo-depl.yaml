# In-cluster MongoDB for the payment service.
# Used for local development (always) and on AWS when
# use_in_cluster_databases = true. For durable production data, prefer
# MongoDB Atlas M0 (free) and point MONGO_URI_PAYMENT at it instead.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: payment-mongo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-mongo-depl
spec:
  replicas: 1
  # Recreate avoids two pods fighting over the same RWO volume during updates.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: payment-mongo
  template:
    metadata:
      labels:
        app: payment-mongo
    spec:
      containers:
        - name: payment-mongo
          image: mongo:7
          volumeMounts:
            - name: data
              mountPath: /data/db
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            tcpSocket:
              port: 27017
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 27017
            initialDelaySeconds: 15
            periodSeconds: 20
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: payment-mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: payment-mongo-srv
spec:
  selector:
    app: payment-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
